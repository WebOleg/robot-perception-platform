name: CI

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches:
      - main

jobs:
  hygiene:
    name: Repo hygiene
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify domain structure exists
        run: |
          for dir in robot bridge services web infra docs; do
            if [ ! -d "$dir" ]; then
              echo "::error::Missing required domain directory: $dir"
              exit 1
            fi
          done
          echo "All domain directories present."

      - name: Ensure no committed secrets (.env)
        run: |
          if git ls-files | grep -E '(^|/)\.env$'; then
            echo "::error::A .env file is committed. Secrets must never be in git."
            exit 1
          fi
          echo "No committed .env files. Good."

      - name: Validate YAML files
        run: |
          python3 -c "import yaml, sys, glob
          files = glob.glob('**/*.yml', recursive=True) + glob.glob('**/*.yaml', recursive=True)
          for f in files:
              with open(f) as fh:
                  yaml.safe_load(fh)
              print('OK:', f)
          print('All YAML valid.')"

  backend:
    name: Backend (api)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies (with dev/test)
        run: pip install -r services/api/requirements-dev.txt

      - name: Syntax check (compile all Python)
        run: python -m compileall services/api/app

      - name: Run tests
        working-directory: services/api
        run: python -m pytest -v

      - name: Build Docker image
        run: docker build -t rpp-api-ci services/api

  robot:
    name: Robot (ROS2)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Syntax check (compile node sources)
        run: python3 -m compileall robot/ros2_ws/src

      - name: Build ROS2 image (colcon build runs inside)
        run: docker build -t rpp-robot-ci robot

  bridge:
    name: Bridge (seam)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Syntax check (compile bridge sources)
        run: python3 -m compileall bridge/ros2_ws/src

      - name: Build bridge image (colcon build runs inside)
        run: docker build -t rpp-bridge-ci bridge
