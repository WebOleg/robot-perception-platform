# Orchestrates the whole platform. Grows service by service.
# Principle #6: everything containerized, started with one command.
# Principle #5: secrets come from .env, never hardcoded here.

services:
  db:
    image: postgres:16-alpine
    container_name: rpp-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  api:
    build: ./services/api
    container_name: rpp-api
    ports:
      - "8080:8080"
    environment:
      API_PORT: 8080
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  # World A — robot domain. Publishes to /heartbeat.
  robot:
    build: ./robot
    container_name: rpp-robot
    command: ros2 launch perception demo.launch.py
    environment:
      ROS_DOMAIN_ID: 0          # same domain so nodes discover each other
    restart: unless-stopped

  # The seam — subscribes to /heartbeat, forwards detections to the API.
  bridge:
    build: ./bridge
    container_name: rpp-bridge
    command: ros2 run bridge_node bridge
    environment:
      ROS_DOMAIN_ID: 0          # same domain as robot
      BRIDGE_TARGET_URL: http://api:8080
    depends_on:
      - api
      - robot
    restart: unless-stopped

volumes:
  db-data:
