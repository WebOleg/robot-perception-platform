# Copy to .env and fill real values. .env is gitignored — never commit it.

# ---- World B: database (single source of truth) ----
POSTGRES_USER=robot
POSTGRES_PASSWORD=change_me
POSTGRES_DB=perception

# ---- World B: backend API ----
API_PORT=8080

# ---- The seam: where bridge sends detections ----
BRIDGE_TARGET_URL=http://api:8080
